# Qwen3.5 (model_type: qwen3_5, ForConditionalGeneration) needs transformers>=5.4.
# For plain Qwen2.5/Qwen3 dense text models, transformers>=4.51 is enough.
transformers>=5.4.0
peft>=0.13.0
# assistant_only_loss + Qwen3 template auto-patch land in newer TRL.
trl>=0.20.0
bitsandbytes>=0.44.0
accelerate>=1.0.0
datasets>=3.0.0

# Optional fast kernels for Qwen3.5's Gated DeltaNet (linear-attention) layers.
# Require Ampere+ (CC>=8.0); on Turing (T4) the model falls back to slow PyTorch
# ops. Install only if your GPU supports them:
#   causal_conv1d
#   fla
